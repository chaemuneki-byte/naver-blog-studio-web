# 검증 결과

2026-09-14 기준.

- Node 자동 테스트 **17개 통과**: 한글/이모지 글자 수, 분량·키워드 검사, 보정 제한, 중지, API 오류, 도메인/탭/프레임 제한, 발행 기록, 중복·재승인 방지.
- 네트워크를 차단한 모의 편집기 테스트 **5개 통과**: 한글/이모지 순차 입력, 최종 클릭 1회, 기존 원고 보호, 입력만 모드, 발행 승인 실패 및 중지.
- 모의 API를 사용하는 웹 통합 테스트 **1개 통과**: 생성 → 미리보기 → 편집 → 분량 오류 시 발행 불가, 모바일 가로 넘침 없음, 키 미저장/새로고침 후 제거.
- 로컬 브라우저에서 첫 화면, 예시 원고, 길이 표시를 직접 확인했습니다.

실제 네이버 계정의 최종 발행, 실제 OpenAI API 호출은 미검증입니다. 실제 계정에 시험 게시물을 올리지 않았습니다. 확장의 네이버 선택자는 화면 변경의 영향을 받을 수 있습니다.

테스트 실행:

```sh
npm test
python -m pip install playwright
python tests/browser_fixture.py
# 별도 터미널에서 npm run serve 실행 후
python tests/web_fixture.py
```

Python 브라우저 테스트는 Microsoft Edge를 사용합니다.
